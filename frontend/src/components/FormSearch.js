import React from 'react';

// const FormSearch = ({ locationMap, zoomLevel } : { locationMap:any, zoomLevel:any}) => (
const FormSearch = () => (
    <div className="formSearch">
        <form>
            <input type="text" name="search" placeholder="Pesquisar por HashTag" />
        </form>
    </div>
)

export default FormSearch
